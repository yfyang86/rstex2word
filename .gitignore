# Code-review notes: a dev-branch artifact, must not land on main (the release
# branch). Kept on the dev PR only; drop before/at promotion to main.
CoderReview/

# Local config with real secrets (commit config.sample.toml instead)
config.toml

# server.sh runtime artifacts
.server.pid
server.log

# default SQLite project store (metasqlite_db); tests use a temp DB
tex2word.db
*.db

# auth signing keys (generated by manage.py init-keys — never commit)
*.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.python-version

# uv / venv
.venv/

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
/target
*/target/

# build artifacts
*.docx
!tests/**/*.docx


# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
